#include <Home.h>
#include <Cat.h>
#include <Dog.h>
#include <iostream>
#include <vector>

int main()
{
	Home home;

	bool quit = false;
	while (!quit)
	{
		std::cout << "1. Add Animal";
		std::cout << "2. Display all Animal";
		std::cout << "3. Remove Animal";
		std::cout << "4. Quit";

		unsigned short selection;
		std::cin >> selection;

		switch (selection)
		{
		case 1:
		
		default:
			break;
		}
	}
    
}
